#!/usr/bin/env groovy

podTemplate(label: 'jenkins-agent-1') {
    node(POD_LABEL) {
        stage('Run outer shell') {
            sh 'echo hello world 1'
            podTemplate(POD_LABEL) {
                node(POD_LABEL) {
                    stage('Run outer shell') {
                        sh 'echo hello world 2'
                    }
                }
            }
        }
    }
}